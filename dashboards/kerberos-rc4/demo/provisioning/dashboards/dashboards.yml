apiVersion: 1

providers:
  - name: kerberos-rc4
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    # Editable so you can nudge a panel before taking the screenshot without
    # having to rebuild and restart.
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
